app: &app
  image: "gtaveras/hound"
  volumes:
    - .:/usr/src/app
  working_dir: '/usr/src/app'
  links:
    - db
    - cache

db:
  image: postgres
  environment:
    POSTGRES_USER: hound
  expose:
    - "5432"

cache:
  image: redis
